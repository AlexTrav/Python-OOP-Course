# «десь объ€вл€етс€ класс Factory

class Loader:
    def parse_format(self, string, factory):
        seq = factory.build_sequence()
        for sub in string.split(","):
            item = factory.build_number(sub)
            seq.append(item)

        return seq


# эти строчки не мен€ть!
ld = Loader()
s = input()
res = ld.parse_format(s, Factory())