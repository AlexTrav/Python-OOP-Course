import sys

# здесь объ€вл€ютс€ все необходимые классы

# считывание списка из входного потока (эту строку не мен€ть)
lst_in = list(map(str.strip, sys.stdin.readlines())) # список lst_in в программе не мен€ть

# здесь создаютс€ объекты классов и вызываютс€ нужные методы